# Система аудиогида Nuanu Creative City

## Обзор

Современная веб-система аудиогида для Nuanu Creative City, разработанная с учетом лучших международных практик UI/UX и accessibility. Система позволяет пользователям получать доступ к аудиогидам через QR-коды без необходимости установки приложения.

## Ключевые функции

### 🎧 Аудиогид с синхронизацией
- HTML5 аудиоплеер с полным набором элементов управления
- Синхронизированная подсветка слов во время воспроизведения
- Поддержка различных скоростей воспроизведения (0.5x - 2.0x)
- Автоматическое сохранение прогресса

### 🌍 Многоязычность
- Поддержка английского, русского и индонезийского языков
- Автоматическое определение языка браузера
- Отдельные аудиодорожки для каждого языка
- Локализованный контент и интерфейс

### 🎥 Видео-экскурсы
- Интеграция с YouTube для видеоконтента
- Встроенный видеоплеер с полноэкранным режимом
- Поддержка субтитров и различных качеств
- Возможность скачивания для офлайн просмотра

### 🗺️ Интерактивная карта
- Интеграция с OpenStreetMap
- Отображение точного местоположения объекта
- Навигация через Google Maps и Apple Maps
- Информация о ближайших объектах

### ♿ Accessibility
- Поддержка screen readers с ARIA-атрибутами
- Режим высокого контраста
- Увеличенные шрифты
- Навигация с клавиатуры
- Информация о доступности каждого объекта

## Архитектура

### Компонентная структура

```
app/audio-guide/[slug]/page.tsx          # Главная страница аудиогида
├── components/audio-guide/
│   ├── AudioGuidePlayer.tsx             # Аудиоплеер с синхронизацией
│   ├── AudioGuideContent.tsx            # Отображение текстового контента
│   ├── AudioGuideMap.tsx                # Интерактивная карта
│   ├── AudioGuideVideo.tsx              # Видеоплеер
│   ├── LanguageSelector.tsx             # Выбор языка
│   └── AccessibilityControls.tsx        # Настройки доступности
├── lib/
│   ├── audio-guide-types.ts             # TypeScript типы
│   └── audio-guide-data.ts              # Данные аудиогидов
└── public/audio/                        # Аудиофайлы
```

### Типы данных

```typescript
interface AudioGuideObject {
  id: string
  slug: string
  coordinates: { lat: number; lng: number }
  image: string
  video?: { youtube_id?: string; url?: string }
  content: { [languageCode: string]: AudioGuideContent }
  category: 'art' | 'history' | 'architecture' | 'nature' | 'culture'
  duration: number
  accessibility: {
    wheelchair_accessible: boolean
    audio_description: boolean
    sign_language: boolean
  }
}
```

## Использование

### 1. Доступ через QR-код
- Пользователь сканирует QR-код рядом с объектом
- Автоматический переход на страницу аудиогида
- URL формат: `/audio-guide/[slug]?lang=[language]`

### 2. Навигация по интерфейсу
- **Content**: Текстовое описание с подсветкой
- **Video**: Видеоконтент об объекте
- **Map**: Интерактивная карта с навигацией
- **More**: Дополнительная информация и факты

### 3. Управление аудио
- Воспроизведение/пауза
- Перемотка на 15 секунд вперед/назад
- Регулировка громкости
- Выбор скорости воспроизведения
- Полноэкранный режим плеера

## Настройка и развертывание

### Требования
- Next.js 14+
- TypeScript
- Tailwind CSS
- Framer Motion
- Lucide React Icons

### Установка
```bash
npm install
# или
yarn install
```

### Добавление нового аудиогида

1. **Создание контента**:
```typescript
// В lib/audio-guide-data.ts
const newGuide: AudioGuideObject = {
  id: 'unique-id',
  slug: 'object-slug',
  coordinates: { lat: -8.3405, lng: 115.0920 },
  image: 'https://example.com/image.jpg',
  content: {
    en: {
      title: 'Object Title',
      description: 'Brief description',
      full_text: 'Full content...',
      audio: {
        url: '/audio/object-slug-en.mp3',
        duration: 180,
        transcript: [/* transcript data */]
      }
    }
  }
}
```

2. **Добавление аудиофайла**:
- Поместите MP3 файл в `public/audio/`
- Обновите URL в данных объекта
- Убедитесь, что transcript соответствует аудио

3. **Создание QR-кода**:
- Используйте URL: `https://yourdomain.com/audio-guide/object-slug?lang=en`
- Разместите QR-код рядом с объектом

### Генерация аудио с помощью TTS

#### Google Cloud Text-to-Speech
```bash
curl -X POST \
  -H "Authorization: Bearer $(gcloud auth application-default print-access-token)" \
  -H "Content-Type: application/json; charset=utf-8" \
  -d @request.json \
  "https://texttospeech.googleapis.com/v1/text:synthesize"
```

#### Amazon Polly
```bash
aws polly synthesize-speech \
  --output-format mp3 \
  --voice-id Joanna \
  --text "Your audio guide text" \
  output.mp3
```

## Лучшие практики

### UX/UI
- Минималистичный дизайн с фокусом на контент
- Крупные кликабельные элементы (минимум 44px)
- Высокий контраст для текста
- Responsive дизайн для всех устройств

### Accessibility
- Все интерактивные элементы доступны с клавиатуры
- ARIA-labels для всех кнопок и элементов управления
- Альтернативный текст для всех изображений
- Поддержка screen readers

### Производительность
- Lazy loading для изображений и видео
- Оптимизированные аудиофайлы (128kbps MP3)
- Минимальный размер JavaScript bundle
- Кэширование статических ресурсов

### SEO
- Семантическая разметка HTML
- Open Graph метатеги
- Structured data для объектов
- Оптимизированные URL

## Интеграция с существующими системами

### CMS интеграция
```typescript
// Пример интеграции с Strapi
const fetchAudioGuides = async () => {
  const response = await fetch('/api/audio-guides')
  return response.json()
}
```

### Аналитика
```typescript
// Google Analytics события
gtag('event', 'audio_guide_start', {
  guide_id: guideId,
  language: currentLanguage
})
```

## Мониторинг и отладка

### Логирование
- Ошибки воспроизведения аудио
- Время загрузки контента
- Пользовательские взаимодействия

### Метрики
- Количество запусков аудиогидов
- Средняя продолжительность прослушивания
- Популярные языки
- Показатели доступности

## Будущие улучшения

### Планируемые функции
- Офлайн режим с Service Workers
- Персонализированные рекомендации
- Социальные функции (отзывы, рейтинги)
- AR интеграция для дополненной реальности
- Голосовое управление
- Интеграция с умными устройствами

### Техническая оптимизация
- WebAssembly для обработки аудио
- PWA функциональность
- Улучшенная синхронизация текста
- Машинное обучение для персонализации

## Поддержка

Для получения помощи или сообщения об ошибках:
- Создайте issue в GitHub репозитории
- Обратитесь к команде разработки
- Проверьте документацию API

---

*Документация обновлена: {{ current_date }}* 